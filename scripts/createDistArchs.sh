# Generate the distribution archives on your local machine by running python setup.py sdist bdist_wheel in your package directory. This will create two files in a dist subdirectory: a source archive (.tar.gz) and a built distribution (.whl).

python setup.py sdist bdist_wheel